$(document).ready(function () {
    if (jQuery) {
        // jQuery is loaded  
        alert("JQuery works, Yeah!");
    } else {
        // jQuery is not loaded
        alert("Doesn't Work");
    }
});